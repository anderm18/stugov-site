import { Box } from "@mui/material";
import React from "react";

export default function Info(): JSX.Element {
    return(
        <Box sx={{backgroundColor: '#fbd0d1', height: '100vh'}}>

        </Box>
    );
}